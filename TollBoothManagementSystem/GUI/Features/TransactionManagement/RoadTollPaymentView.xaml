<UserControl x:Class="TollBoothManagementSystem.GUI.Features.TransactionManagement.RoadTollPaymentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TollBoothManagementSystem.GUI.Features.TransactionManagement"
             Background="White"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <GroupBox Height="130" Header="Vehicle category" FontSize="16" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <RadioButton Content="Category 1a" IsChecked="True" Grid.Column="0" Style="{StaticResource VehicleCategoryButtonTheme}"/>
                <RadioButton Content="Category 1" IsChecked="False" Grid.Column="1" Style="{StaticResource VehicleCategoryButtonTheme}"/>
                <RadioButton Content="Category 2" IsChecked="False" Grid.Column="2" Style="{StaticResource VehicleCategoryButtonTheme}"/>
                <RadioButton Content="Category 3" IsChecked="False" Grid.Column="3" Style="{StaticResource VehicleCategoryButtonTheme}"/>
                <RadioButton Content="Category 4" IsChecked="False" Grid.Column="4" Style="{StaticResource VehicleCategoryButtonTheme}"/>
            </Grid>
        </GroupBox>
        <TextBlock Text="Currency:" FontSize="16" Grid.Row="1" Margin="0 15 0 0"/>
        <ComboBox ItemsSource="{Binding Currencies}" SelectedItem="{Binding SelectedCurrency}" Grid.Row="2" BorderThickness="2" BorderBrush="#10416b" Margin="0 5 0 0" Width="150" HorizontalAlignment="Left"/>
        <TextBlock Text="Enter station:" FontSize="16" Grid.Row="3" Margin="0 15 0 0"/>
        <ComboBox ItemsSource="{Binding TollStations}" SelectedItem="{Binding SlectedStation}" Grid.Row="4" BorderThickness="2" BorderBrush="#10416b" Margin="0 5 0 0" Width="275" HorizontalAlignment="Left"/>
        <GroupBox Grid.Row="5" Header="Summary" FontSize="16" Margin="0 15 0 0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="{Binding Path=EnterStation.Name, StringFormat=Enter station:{0}}"/>
                <TextBlock Grid.Row="1" Text="{Binding Path=CurrentStation.Name, StringFormat=Exit station:{0}}" Margin="0 10 0 0"/>
                <TextBlock Grid.Row="2" Text="{Binding Path=Price, StringFormat=Price:{0}}" Margin="0 10 0 0"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
